# Vexoo--AI-Assignment
First, take a large document (text file or article) as input. Then, split the document into smaller parts using a sliding window method with overlap between chunks. For each chunk, store the original text as the raw data layer. Select 3000 samples for training and 1000 samples for testing. Load a language model (LLaMA or a small alternative if need
